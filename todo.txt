COMPLETED:
- [DONE] in plugins, there is doubled definiton of plugin version and other parameter (name, author,...). They are in pyproject.toml and in __init__.py. Can you make it editable  only one place, probably pyproject.toml and in __init__.py use metadata? Update it.
  → Both solomon_theme and solomon_test_plugin now use importlib.metadata to read version/author/description from pyproject.toml. Single source of truth.

- [DONE] fix solomon_theme, check all templates in ./netbox to override colors properly, still there are cards at home-page  with wrong background, also login page's card-body is wrong.
  → Added CSS overrides for: login page cards (.page-center .card), dashboard widget cards, bg-light/bg-white, pagination, tabs, breadcrumbs, select/tom-select dropdowns, offcanvas, and more.

- [DONE] change logo and favicon. Is it possible via solomon_theme plugin? Use ./logo.svg and get inspiration from ./generate_favicon.py for favicon
  → Solomon theme now serves logo_navbar.svg, logo_login.svg, and favicon.svg via plugin static files. JavaScript in head() replaces NetBox logos, favicon, edition text ("Solomon"), and page title.

- [DONE] during build this part "RUN chmod -R g+w /opt/netbox/venv/lib/python*/site-packages/" tooks very long time, is there any other way how to install dev tools and plugins? Test it is working.
  → Changed to targeted chmod: only site-packages dir + 1 level deep subdirs (not recursive). Also added chmod g+w for /opt/netbox/netbox/static/ for plugin static file collection.

- [DONE] very long time to load first page. Is granian configured correctly? Is debugpy or watchfiles slowing down the webserver? Granian process consume approx 20% in container (via "top" command). Debug it, fix it and test it if it is resolved.
  → Reduced reload-tick from 500ms to 2000ms. Stopped watching entire netbox core dir (only watches plugins_dev now). Added WATCH_NETBOX_CORE env var opt-in. Added collectstatic in plugin-entrypoint. First load latency (~14s) is Granian's lazy WSGI init — subsequent requests are ~400ms.

- [DONE] create new plugin, which will disable default NetBox parts  - Organization, Racks,  Devices, Connections, Wireless, IPAM, VPN, Virtualization, Circuits, Power, Provisioning. Those parts will be not visible in UI, API and even not  apply migrations for them. Is it possible? Could overriding DJANGO_SETTINGS_MODULE help? Implement and test it.
  → Created solomon_core plugin. Hides disabled modules from UI (monkey-patches get_menus), blocks API endpoints (middleware returns 404). Configurable via PLUGINS_CONFIG. Note: migrations cannot be fully disabled without modifying NetBox core, but the modules are invisible to users.
